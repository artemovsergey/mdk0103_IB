# Практическая работа 2. Условные выражения в TypeScript

## Тема
Использование условных выражений (if/else, switch, тернарный оператор) для управления потоком выполнения программы.

## Цель
Научиться применять условные конструкции if/else и switch для реализации ветвления в программах на TypeScript.

## Ход работы
1. Изучить теоретический материал по условным выражениям.
2. Выполнить практический пример.
3. Решить задание согласно своему варианту.
4. Оформить отчет в соответствии с требованиями.

## Теоретическая часть

### Условные выражения if/else
Конструкция if/else позволяет выполнять различные блоки кода в зависимости от условия:
```ts
if (condition) {
  // код, если условие истинно
} else {
  // код, если условие ложно
}
```

### Множественное ветвление с switch
Конструкция switch заменяет цепочки else if и позволяет обрабатывать несколько условий:
```ts
switch (variable) {
  case value1:
    // действие 1
    break
  case value2:
    // действие 2
    break
  default:
    // действие по умолчанию
}
```

### Тернарный оператор
Тернарный оператор позволяет выбрать одно из двух значений:
```ts
const result = x > 0 ? "Positive" : "Negative"
```

## Практический пример

**Задача**: Написать программу, которая определяет время суток по заданному часу.

```ts
function timeOfDay(hour: number): string {
  if (hour < 6) return "Ночь"
  if (hour < 12) return "Утро"
  if (hour < 18) return "День"
  if (hour < 24) return "Вечер"
  return "Неверное время"
}

const hour = 14
console.log(`Сейчас: ${timeOfDay(hour)}`)
```

### Задания по вариантам

**Вариант 1**: Написать программу, которая определяет знак зодиака по месяцу и дню рождения.

**Вариант 2**: Написать программу, которая вычисляет стоимость билета в кино в зависимости от возраста и времени сеанса.

**Вариант 3**: Написать программу, которая определяет время года по номеру месяца.

**Вариант 4**: Написать программу-калькулятор, который выполняет операции (+, -, *, /) над двумя числами.

**Вариант 5**: Написать программу, которая определяет тип треугольника по длинам сторон.

**Вариант 6**: Написать программу, которая выводит рекомендацию по одежде в зависимости от температуры.

**Вариант 7**: Написать программу, которая определяет високосный год или нет.

**Вариант 8**: Написать программу, которая переводит числовую оценку в буквенную (5->A, 4->B и т.д.).

**Вариант 9**: Написать программу, которая определяет категорию пользователя по возрасту.

**Вариант 10**: Написать программу, которая вычисляет скидку в магазине в зависимости от суммы покупки.

**Вариант 11**: Написать программу, которая определяет тип транспортного средства по количеству колес.

**Вариант 12**: Написать программу, которая рекомендует вид спорта в зависимости от сезона.

**Вариант 13**: Написать программу, которая определяет этап жизни человека по возрасту.

**Вариант 14**: Написать программу, которая вычисляет налог на доход в зависимости от суммы.

**Вариант 15**: Написать программу, которая определяет тип животного по его характеристикам.

## Критерии оценки

**5 (отлично)**:
- Программа работает корректно для всех входных данных
- Код хорошо структурирован и читаем
- Использованы подходящие условные конструкции
- Отчет оформлен полностью и аккуратно

**4 (хорошо)**:
- Программа работает корректно для большинства случаев
- Код в целом читаем, но есть небольшие недочеты
- Отчет оформлен, но с незначительными ошибками

**3 (удовлетворительно)**:
- Программа работает с ограничениями
- Код плохо структурирован
- Отчет оформлен не полностью

**2 (неудовлетворительно)**:
- Программа не работает или работает некорректно
- Отчет отсутствует или оформлен крайне небрежно

## Контрольные вопросы

1. В чем разница между операторами `if` и `switch`?
2. Когда целесообразно использовать `switch` вместо `if/else`?
3. Что такое тернарный оператор и как его использовать?
4. Как проверить, что число находится в диапазоне?
5. Что такое строгое равенство `===` и чем оно отличается от `==`?
6. Как проверить значение на `null` и `undefined`?
7. Что делает оператор `??` (нулевое слияние) и когда его использовать?

## Требования к оформлению

### Структура репозитория:
```
practical-work-3/
├── main.ts
├── images/
│   ├── screenshot1.png
│   └── screenshot2.png
├── .gitignore
└── README.md
```

**README.md**:
````markdown
# Практическая работа №3: Условные выражения
## Вариант: 1
## Задание: Написать программу, которая определяет знак зодиака по месяцу и дню рождения.

### Код программы:

### Скриншоты работы программы:
![Результат для Овна](images/screenshot1.png)
![Результат для неверной даты](images/screenshot2.png)
````
