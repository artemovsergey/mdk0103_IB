# Практическая работа 1. Основы TypeScript - переменные и функции

## Тема
Создание и использование переменных и функций в TypeScript.

## Цель
Закрепить знания по работе с переменными и функциями в TypeScript, научиться писать простые программы с использованием основных типов данных, объявлять и вызывать функции.

## Ход работы
1. Изучить теоретический материал по переменным и функциям.
2. Выполнить практический пример.
3. Решить задание согласно варианту.
4. Оформить отчет в виде проекта с файлом `main.ts`, `README.md` и скриншотами работы программы.

## Теоретическая часть

### Переменные в TypeScript
Переменная - это контейнер для хранения данных. В TypeScript переменные объявляются с помощью ключевых слов `const` (неизменяемая) и `let` (изменяемая).

```ts
const name: string = "Иван"  // неизменяемая переменная
let age: number = 25         // изменяемая переменная
```

### Функции в TypeScript
Функция - это блок кода, который выполняет определенную задачу. Функции объявляются с помощью ключевого слова `function`.

```ts
function greet(name: string): string {
  return `Привет, ${name}!`
}
```

## Практический пример

**Задача:** Написать программу, которая вычисляет площадь прямоугольника.

```ts
function calculateArea(width: number, height: number): number {
  return width * height
}

const width = 10
const height = 5
const area = calculateArea(width, height)
console.log(`Площадь прямоугольника: ${area}`)
```

**Вывод:**
```
Площадь прямоугольника: 50
```

Запуск программы:

```bash
node main.ts
```

## Задания

### Вариант 1
Напишите программу, которая вычисляет периметр прямоугольника.

### Вариант 2
Напишите программу, которая конвертирует градусы Цельсия в Фаренгейты.

### Вариант 3
Напишите программу, которая вычисляет сумму цифр двухзначного числа.

### Вариант 4
Напишите программу, которая проверяет, является ли число четным.

### Вариант 5
Напишите программу, которая находит максимальное из трех чисел.

### Вариант 6
Напишите программу, которая вычисляет факториал числа.

### Вариант 7
Напишите программу, которая определяет, является ли строка палиндромом.

### Вариант 8
Напишите программу, которая вычисляет НОД двух чисел.

### Вариант 9
Напишите программу, которая вычисляет среднее арифметическое трех чисел.

### Вариант 10
Напишите программу, которая проверяет, является ли год високосным.

### Вариант 11
Напишите программу, которая переворачивает строку.

### Вариант 12
Напишите программу, которая вычисляет площадь круга.

### Вариант 13
Напишите программу, которая определяет, является ли число простым.

### Вариант 14
Напишите программу, которая вычисляет длину гипотенузы прямоугольного треугольника.

### Вариант 15
Напишите программу, которая генерирует случайное число в заданном диапазоне.

## Критерии оценки

### 5 (отлично)
- Программа выполняется без ошибок.
- Задание выполнено полностью и корректно.
- Код хорошо отформатирован и содержит комментарии.
- Скриншоты работы программы приложены.

### 4 (хорошо)
- Программа выполняется без ошибок.
- Задание выполнено с незначительными недочетами.
- Код читаем, но есть небольшие нарушения стиля.

### 3 (удовлетворительно)
- Программа выполняется, но содержит логические ошибки.
- Задание выполнено частично.
- Код плохо отформатирован.

### 2 (неудовлетворительно)
- Программа не выполняется.
- Задание не выполнено.

## Контрольные вопросы
1. В чем разница между `const` и `let`?
2. Какие основные типы данных в TypeScript вы знаете?
3. Как объявить функцию с параметрами и возвращаемым значением?
4. Что такое шаблонные строки и как их использовать?
5. Как работать с аргументами по умолчанию в функциях?

---

## Структура репозитория
```
pr01-typescript-basics/
├── main.ts
├── images/
│   ├── screenshot1.png
│   └── screenshot2.png
├── .gitignore
└── README.md
```

## Пример файла README.md для варианта 1

````markdown
# Практическая работа №1: Основы TypeScript - переменные и функции

## Вариант 1: Вычисление периметра прямоугольника

### Задание
Напишите программу, которая вычисляет периметр прямоугольника.

### Код программы
```ts
function calculatePerimeter(length: number, width: number): number {
  return 2 * (length + width)
}

const length = 10
const width = 5
const perimeter = calculatePerimeter(length, width)
console.log(`Периметр прямоугольника: ${perimeter}`)
```

### Скриншоты работы программы
![Результат выполнения](images/screenshot.png)
````
