192.168.255 192.168.4
Ставропольский колледж связи
Специальность::0907::Информационные системы и программирование
Дисциплина::2003::МДК0103 Разработка мобильных приложений: Коллекции и функции высшего порядка
Преподаватель::Артемов С.В.
Индекс теста::3::МДК0103 Разработка мобильных приложений: Коллекции и функции высшего порядка
Всего вопросов::40
Выводимых вопросов::35
Тип теста::S=100::Z(DCDCDC)::J80%70%60%

##01::3
Что такое массив в Kotlin?

Упорядоченная коллекция элементов одного типа с фиксированным размером
Неупорядоченная коллекция элементов разных типов
Коллекция пар ключ-значение

##02::3
Как создать массив строк в Kotlin?

arrayOf("a", "b", "c")
listOf("a", "b", "c")
setOf("a", "b", "c")

##03::3
Как получить доступ к элементу массива по индексу?

array[0]
array.get(0)
array(0)

##04::3
Что произойдет при попытке доступа к несуществующему индексу массива?

ArrayIndexOutOfBoundsException
Вернет null
Ничего не произойдет

##05::3
Как объединить два массива в Kotlin?

array1 + array2
array1.plus(array2)
array1.concat(array2)

##06::3
Чем отличается List от MutableList?

List неизменяем, MutableList можно изменять
List можно изменять, MutableList неизменяем
Разницы нет

##07::3
Как создать неизменяемый список в Kotlin?

listOf(1, 2, 3)
mutableListOf(1, 2, 3)
arrayOf(1, 2, 3)

##08::3
Как добавить элемент в MutableList?

list.add(element)
list.plus(element)
list.insert(element)

##09::3
Как удалить элемент из MutableList по значению?

list.remove(element)
list.delete(element)
list.removeAt(element)

##10::3
Как проверить наличие элемента в списке?

element in list
list.contains(element)
list.has(element)

##11::3
Что делает метод indexOf() в списке?

Возвращает индекс первого вхождения элемента
Возвращает индекс последнего вхождения элемента
Возвращает все индексы элемента

##12::3
Какой цикл используется для итерации по коллекции?

for (element in collection)
while (element in collection)
do-while (element in collection)

##13::3
Что такое Set в Kotlin?

Неупорядоченная коллекция уникальных элементов
Упорядоченная коллекция элементов
Коллекция пар ключ-значение

##14::3
Как создать изменяемый набор в Kotlin?

mutableSetOf(1, 2, 3)
setOf(1, 2, 3)
arrayOf(1, 2, 3)

##15::3
Что произойдет при попытке добавить дубликат в Set?

Элемент не добавится
Элемент заменит существующий
Произойдет ошибка

##16::3
Как удалить элемент из MutableSet?

set.remove(element)
set.delete(element)
set.removeAt(element)

##17::3
Что такое Map в Kotlin?

Коллекция пар ключ-значение
Упорядоченная коллекция элементов
Неупорядоченная коллекция уникальных элементов

##18::3
Как создать изменяемую карту в Kotlin?

mutableMapOf("key" to "value")
mapOf("key" to "value")
arrayOf("key" to "value")

##19::3
Как получить значение из Map по ключу?

map["key"]
map.get("key")
map.value("key")

##20::3
Что вернет map["nonexistent"] для несуществующего ключа?

null
Исключение
Пустую строку

##21::3
Как добавить пару ключ-значение в MutableMap?

map["key"] = value
map.put("key", value)
map.add("key", value)

##22::3
Как удалить пару ключ-значение из MutableMap?

map.remove("key")
map.delete("key")
map.removeAt("key")

##23::3
Что такое функция высшего порядка?

Функция, принимающая другую функцию как параметр или возвращающая функцию
Функция высшего порядка выполнения
Функция с большим количеством параметров

##24::3
Что делает функция forEach()?

Выполняет действие для каждого элемента коллекции
Фильтрует элементы коллекции
Преобразует элементы коллекции

##25::3
Как вставить выражение в строку в Kotlin?

${expression}
#{expression}
$expression

##26::3
Что делает функция map()?

Преобразует каждый элемент коллекции
Фильтрует элементы коллекции
Группирует элементы коллекции

##27::3
Что делает функция filter()?

Создает новую коллекцию с элементами, удовлетворяющими условию
Преобразует каждый элемент коллекции
Группирует элементы коллекции

##28::3
Что делает функция groupBy()?

Группирует элементы по заданному критерию
Фильтрует элементы коллекции
Преобразует каждый элемент коллекции

##29::3
Что делает функция fold()?

Преобразует коллекцию в единственное значение
Группирует элементы коллекции
Фильтрует элементы коллекции

##30::3
Что делает функция sortedBy()?

Сортирует коллекцию по заданному свойству
Фильтрует коллекцию
Группирует коллекцию

##31::3
Какой тип возвращает map()?

List с преобразованными элементами
Тот же тип коллекции
Единственное значение

##32::3
Какой тип возвращает filter()?

Тот же тип коллекции
List с преобразованными элементами
Единственное значение

##33::3
Какой тип возвращает groupBy()?

Map с группированными элементами
List с преобразованными элементами
Единственное значение

##34::3
Какой тип возвращает fold()?

Единственное значение
Map с группированными элементами
List с преобразованными элементами

##35::3
Как отсортировать список строк по алфавиту?

list.sortedBy { it }
list.filter { it }
list.map { it }

##36::3
Как найти сумму всех элементов списка чисел?

list.fold(0) { acc, num -> acc + num }
list.map { it + it }
list.filter { it > 0 }

##37::3
Как получить список только четных чисел из списка?

list.filter { it % 2 == 0 }
list.map { it * 2 }
list.groupBy { it % 2 }

##38::3
Как сгруппировать список строк по первой букве?

list.groupBy { it.first() }
list.filter { it.first() }
list.map { it.first() }

##39::3
Как преобразовать список чисел в список их квадратов?

list.map { it * it }
list.filter { it * it }
list.groupBy { it * it }

##40::3
Как вывести каждый элемент списка на новой строке?

list.forEach { println(it) }
list.map { println(it) }
list.filter { println(it) }