192.168.255 192.168.4
Ставропольский колледж связи
Специальность::0907::Информационные системы и программирование
Дисциплина::2003::МДК0103 Разработка мобильных приложений: Типы функций, работа с коллекциями
Преподаватель::Артемов С.В.
Индекс теста::5::МДК0103 Разработка мобильных приложений: Типы функций, работа с коллекциями
Всего вопросов::40
Выводимых вопросов::35
Тип теста::S=100::Z(DCDCDC)::J80%70%60%

##01::3
Что такое функции первого класса в Kotlin?

Функции, которые можно хранить в переменных и передавать как данные
Функции, которые объявляются с помощью ключевого слова fun
Функции, которые возвращают значения
Функции, которые принимают параметры

##02::3
Какой оператор используется для ссылки на функцию как на значение?

::
-&gt
it
fun

##03::3
Что такое лямбда-выражение?

Сокращенный синтаксис для определения функции без ключевого слова fun
Функция, объявленная с помощью ключевого слова fun
Функция, которая возвращает другое значение
Функция с большим количеством параметров

##04::3
Какой тип данных у функции: () -&gt Unit?

Функция без параметров, которая ничего не возвращает
Функция с одним параметром, возвращающая Unit
Функция с двумя параметрами
Функция, возвращающая String

##05::3
Что такое функция высшего порядка?

Функция, которая принимает или возвращает другие функции
Функция с большим количеством параметров
Функция, которая вызывается много раз
Функция, объявленная в классе

##06::3
Как объявить nullable тип функции?

(() -&gt Unit)?
()-&gtUnit?
(()-&gtUnit)?
Unit? -&gt ()

##07::3
Какое неявное имя параметра используется в лямбдах с одним параметром?

it
this
param
value

##08::3
Что такое синтаксис trailing lambda?

Размещение лямбда-выражения после круглых скобок вызова функции
Размещение лямбды внутри круглых скобок
Объявление лямбды до вызова функции
Использование ключевого слова lambda

##09::3
Какой результат вернет лямбда-выражение?

Значение последнего выражения в теле
Значение первого выражения
Всегда Unit
Значение переменной it

##10::3
Для чего используется функция repeat()?

Для повторения кода заданное количество раз
Для возврата значения из функции
Для объявления переменных
Для создания классов

##11::3
Какая функция используется для перебора элементов коллекции?

forEach()
map()
filter()
fold()

##12::3
Как правильно вставить выражение в строковый шаблон?

${expression}
$expression
{expression}
expression$

##13::3
Что делает функция map() с коллекцией?

Преобразует каждый элемент в новый элемент
Фильтрует элементы по условию
Группирует элементы по ключу
Суммирует все элементы

##14::3
Какой тип коллекции возвращает функция map()?

Новая коллекция того же размера
Коллекция того же или меньшего размера
Всегда список
Всегда карта

##15::3
Что делает функция filter()?

Создает подмножество коллекции на основе условия
Преобразует тип элементов коллекции
Сортирует коллекцию
Группирует элементы

##16::3
Какой тип возвращает filter() для List&ltCookie&gt?

List&ltCookie&gt
List&ltString&gt
Map&ltBoolean, List&ltCookie&gt&gt
Unit

##17::3
Что делает функция groupBy()?

Разделяет коллекцию на группы по ключу
Фильтрует элементы коллекции
Преобразует типы элементов
Сортирует коллекцию

##18::3
Какой тип возвращает groupBy { it.softBaked } для List&ltCookie&gt?

Map&ltBoolean, List&ltCookie&gt&gt
List&ltBoolean&gt
List&ltCookie&gt
Map&ltString, List&ltCookie&gt&gt

##19::3
Для чего используется функция fold()?

Для преобразования коллекции в одно значение
Для фильтрации коллекции
Для сортировки коллекции
Для группировки коллекции

##20::3
Какие параметры принимает fold()?

Начальное значение и лямбда-выражение
Только лямбда-выражение
Два лямбда-выражения
Начальное значение и предикат

##21::3
Что такое аккумулятор в функции fold()?

Промежуточный итог вычислений
Конечный результат
Тип коллекции
Параметр лямбды

##22::3
Как отсортировать коллекцию по свойству?

sortedBy { it.property }
sort { it.property }
filter { it.property }
map { it.property }

##23::3
Какой тип возвращает sortedBy()?

Тот же тип коллекции, но отсортированный
Новый тип коллекции
Единичное значение
Карту

##24::3
Как получить доступ к свойствам объекта в строковом шаблоне внутри лямбды?

${it.property}
$it.property
{it.property}
it.property$

##25::3
Что выведет код: val result = listOf(1,2,3).map { it * 2 }?

[2, 4, 6]
[1, 2, 3]
[2, 2, 2]
Ошибка компиляции

##26::3
Как отфильтровать только четные числа из списка?

filter { it % 2 == 0 }
filter { it % 2 == 1 }
map { it % 2 == 0 }
forEach { it % 2 == 0 }

##27::3
Что делает оператор % в Kotlin?

Возвращает остаток от деления
Возвращает результат деления
Умножает числа
Сравнивает числа

##28::3
Как сгруппировать числа на четные и нечетные?

groupBy { it % 2 }
filter { it % 2 }
map { it % 2 }
forEach { it % 2 }

##29::3
Как вычислить сумму всех элементов списка чисел?

fold(0) { acc, num -&gt acc + num }
filter { it &gt 0 }
map { it.sum() }
forEach { it + it }

##30::3
Что такое лямбда с параметрами?

{ param1, param2 -&gt ... }
{ -&gt ... }
{ it -&gt ... }
{ ... }

##31::3
Как объявить функцию, принимающую лямбду как параметр?

fun process(action: () -&gt Unit)
fun process(action: Unit)
fun process(action: String)
fun process(): () -&gt Unit

##32::3
Как объявить функцию, возвращающую лямбду?

fun createAction(): () -&gt Unit
fun createAction(): Unit
fun createAction() -&gt Unit
fun createAction(): { -&gt Unit }

##33::3
Что такое функция forEach()?

Выполняет действие для каждого элемента коллекции
Фильтрует элементы коллекции
Преобразует элементы коллекции
Сортирует коллекцию

##34::3
Какой тип имеет переменная: val action = { println("Hello") }?

() -&gt Unit
() -&gt String
(String) -&gt Unit
Unit

##35::3
Что означает Unit в типе функции?

Функция ничего не возвращает
Функция возвращает строку
Функция возвращает число
Функция имеет параметры

##36::3
Как передать лямбду непосредственно в вызов функции?

function { /* body */ }
function({ /* body */ })
function -&gt { /* body */ }
function: { /* body */ }

##37::3
Что делает функция sum() для числовых коллекций?

Возвращает сумму всех элементов
Фильтрует положительные числа
Преобразует в строки
Сортирует по возрастанию

##38::3
Как создать пустой список при null значении из Map?

?: listOf()
!!.listOf()
?.listOf()
listOf() ?:

##39::3
Что такое функция partition()?

Разделяет коллекцию на две по условию
Объединяет две коллекции
Преобразует коллекцию
Фильтрует коллекцию

##40::3
Как отсортировать строки по алфавиту?

sortedBy { it }
filter { it }
map { it }
forEach { it }